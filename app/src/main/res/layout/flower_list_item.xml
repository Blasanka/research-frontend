<?xml version="1.0" encoding="utf-8"?>
<androidx.cardview.widget.CardView
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:background="@color/white"
    app:cardCornerRadius="10dp"
    app:cardPreventCornerOverlap="false"
    android:clipToPadding="false"
    app:elevation="4dp"
    app:cardBackgroundColor="@color/white"
    android:layout_marginHorizontal="10dp"
    android:layout_marginTop="5dp">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginHorizontal="10dp"
        android:layout_marginVertical="15dp"
        android:orientation="horizontal"
        android:gravity="center_vertical">

        <ImageView
            android:id="@+id/flowerIv"
            android:transitionName="flowerNameIv"
            android:layout_width="70dp"
            android:layout_height="70dp"
            android:src="@drawable/default_sifaa_image"
            android:scaleType="centerCrop" />

        <LinearLayout
            android:layout_width="0dp"
            android:layout_weight="1"
            android:layout_height="wrap_content"
            android:layout_marginStart="15dp"
            android:orientation="vertical">

            <TextView
                android:id="@+id/flowerNameTv"
                android:transitionName="flowerName"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:maxLength="15"
                android:text="@string/flower_rose"
                android:fontFamily="@font/montserrat_semi_bold"
                android:textColor="@color/text_dark_color"
                android:textSize="18sp"/>

            <TextView
                android:id="@+id/flowerCatTv"
                android:transitionName="flowerCat"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:text="@string/flower_info"
                android:ellipsize="end"
                android:fontFamily="@font/montserrat_regular"
                android:textSize="12sp"/>

<!--            android:maxLength="15"-->
<!--            android:maxLines="2"-->

        </LinearLayout>

    </LinearLayout>

</androidx.cardview.widget.CardView>